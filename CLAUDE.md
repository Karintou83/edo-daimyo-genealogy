# CLAUDE.md

このリポジトリで作業するときの前提・方針をまとめたもの。新しい会話やセッションでこのプロジェクトを開いたら、まずこのファイルを読むこと。

## プロジェクトの目的

江戸時代の藩主を血縁関係でつないだ、インタラクティブな家系図サイトを作る。最終的にGitHub(Pages)で公開する。

## 確定している設計方針

- **つなぐ関係**: 実際の血縁(親子)関係のみ。養子として藩主になった人物は、養父ではなく実父とつなぐ。
- **ノードの情報**: 氏名+その人物のWikipedia記事へのリンク+務めた役職の一覧、の構成。生年没年は不要。役職は `positions: [{han, generation, year_start, year_end}, ...]` という配列で持たせる(1人が複数の藩/将軍職を務めた場合に対応するため。詳細は「役職の併記」の項を参照)。家系図上のノード名表示は「和歌山藩5代目・将軍8代目」のように役職を連結したものでよく、在位年(year_start/year_end)は常時表示はせず、ノードクリック時の詳細パネルに表示する。在位年が取得できない場合は`year_start`/`year_end`をnullにする(必須項目ではない)。
- **データ取得元**: 対象人物と代数(藩主として何代目か)は、日本語版Wikipediaの藩主継承テンプレート(例: `{{佐賀藩主}}`)から取得する(テンプレート内の並び順=代数)。**在位年(year_start/year_end)もこのテンプレートから取得する**: テンプレート内に「1590-1601」のような年数が書かれていれば、そこから抽出する(古河藩のような表形式テンプレートで特に有用)。テンプレートから取れない場合は、WikidataのP39(役職)プロパティの開始日・終了日をフォールバックとして使う。在位年は`positions`配列の並び順(時系列順)を決める材料としても使う。**父の情報はWikidataを最優先で使う**: 各記事のページ情報からWikidataのQID(項目ID)を取得し、WikidataのP22(父)プロパティを問い合わせる。P22が未登録の場合のみ、Wikipedia記事のInfobox(基礎情報系テンプレート)の「父」パラメータ(実父/養父の書き分けがあれば実父を優先)をフォールバックとして使う。ただしフォールバック時も、Infobox中のリンク先(表示テキストではなくリンクターゲットの記事名)から父の記事を特定し、その記事のQIDを取得して以降はQIDで扱う。
- **人物の識別**: すべての人物はWikidataのQIDを主キーとして扱う(氏名の文字列では照合しない)。同姓同名の人物が別々に存在しても、QIDで区別すれば取り違えない。記事にWikidata項目が存在しない稀なケースのみ、Wikipedia記事名をIDとして代用する。
- **サイトの構成**: データは一度取得してJSONファイルに保存し、サイトはそのJSONを読み込むだけの静的な構成にする。ページ表示のたびにWikipedia APIへ問い合わせるような、リアルタイム取得の仕組みにはしない。データを更新したい場合は取得スクリプトを再実行してJSONを作り直す。
- **表示**: D3.jsによるインタラクティブな家系図(ズーム・パン、ノードクリックで詳細パネル)。**色分けは「実際にその藩の藩主だったかどうか」で決める**: `is_stub: false`(藩主一覧テンプレートに載っている実際の藩主)のノードのみ、藩ごとの色を塗る。`is_stub: true` のノードは、`positions` の中身に関わらず(たとえ対象藩に属する人物であっても)藩主とは異なる見た目(グレーアウト・破線枠など)にし、藩主本人ではないことが一目でわかるようにする。`positions` が複数ある人物(徳川吉宗など)の色分けは、`positions`の最初の要素の藩を代表色として使うか、複数色を併記するか、フェーズ3実装時に決める(未確定事項)。
- **対象藩以外の実父の扱い**: 実父が対象藩の藩主一覧に含まれない場合(例: 上杉鷹山の実父・秋月種美は高鍋藩主)、その人物を「スタブノード」として追加する。氏名・Wikipediaリンク・(取得できれば)藩名のみを持たせ、そこからさらに祖先を遡って展開はしない。表示上も通常ノードと区別できるようにする(薄い色・簡易表示など)。
- **画像**: WikidataのP18(image)プロパティを使い、画像がある人物には肖像画像を表示する。画像がない人物は画像なしの表示にフォールバックする。
- **取得データのファイル分割**: 対象藩を増やしていく運用を見据え、フェーズ1の生データは藩ごとに別ファイル(`data/raw/<藩の英語表記スラッグ>.json`、例: `data/raw/yonezawa.json`)に分けて保存する。表示用の統合ファイル(`data/tree.json`)は今まで通り単一ファイルのままにする(D3.jsでの表示は血縁が藩をまたぐため、全体を一度に持つ必要があるが、想定データ量では1〜2MB程度に収まる見込みで問題ない)。

## プロトタイプ対象

まず数藩から検証する。選定理由もあわせて記載。

- 米沢藩(上杉家): 上杉鷹山が秋月家からの養子であり、養子ケース(実父リンク)の検証に使う
- 加賀藩(前田家): 直系継承が多く、シンプルなケースの検証に使う

対象藩を増やす際は、フェーズ1のスクリプトに藩名リストを渡せるようにしておくことで拡張する想定。

## 実装フェーズ

1. **フェーズ1(データ取得)**: `scripts/` に、Wikipedia上の藩主継承テンプレートとWikidataからデータを取得し、藩ごとに `data/raw/<藩の英語表記スラッグ>.json` に保存するスクリプトを作る。各人物は `id(QID), name, positions: [{han, generation, year_start, year_end}], wikipedia_url, image_url, father_id(QID), is_stub: false` を持たせる(`positions`はその藩のテンプレートから見つかった分の1要素。他の藩でも見つかった場合はフェーズ2で配列に統合する)。`year_start`/`year_end`はテンプレート内の年数表記(例:「1590-1601」)から取得し、なければWikidataのP39(開始日・終了日)をフォールバックとして使う。取れなければnull。父の判定はWikidataのP22を最優先、未登録ならInfoboxの「父」パラメータをフォールバックとして使う(フォールバック時もリンクターゲットからQIDを解決する)。画像はWikidataのP18(image)からCommonsのファイル名を取得し、`https://commons.wikimedia.org/wiki/Special:FilePath/<ファイル名>?width=200` の形でimage_urlを組み立てる(P18がなければnull)。父が対象藩の藩主一覧に含まれない場合は、その父を `is_stub: true` の人物として追加し、`id(QID), name, wikipedia_url, image_url, positions: [{han, generation: null, year_start, year_end}](藩名が取得できれば) または [], father_id: null, is_stub: true` のみを持たせ、その人物の父はさらに遡って取得しない。
2. **フェーズ2(データ整形)**: `data/raw/*.json` をすべて読み込んで統合し、`nodes`(id, name, positions, wikipedia_url, image_url, is_stub)と `edges`(parent_id, child_id)を組み立て、`data/tree.json` に保存するスクリプトを作る。複数のraw ファイルに同じQIDの人物が重複して出てくることがある。
   - スタブと正式レコードが重複する場合(例: 秋月種美が現状スタブだが、高鍋藩を対象に加えると正式レコードになる)は、`is_stub: false` の方を採用する。
   - 正式レコード同士が重複する場合(転封・徳川吉宗のような兼務など)は、**どちらか一方を選ばず、`positions` 配列に両方をマージする**(例: 徳川吉宗は `positions: [{han: "紀州藩", generation: 5, year_start: ..., year_end: ...}, {han: "徳川将軍家", generation: 8, ...}]`)。`positions` の順序は、`year_start`(取得できていればテンプレート由来、なければWikidataのP39由来)で時系列順に並べる。
3. **フェーズ3(表示)**: `tree.json` を読み込んでD3.jsで表示する `index.html` を作る。家系図上のノード名表示は `positions` の各要素を「○○藩○代目」の形にして「・」で連結する(例: 「紀州藩5代目・将軍8代目」)。`positions` が空(han不明のスタブ)の場合は氏名のみ表示する。ノードクリック時の詳細パネルには、役職ごとに在位年(year_start〜year_end)も表示する。`image_url` があれば肖像画像を表示し、なければ画像なしの表示にする。

## 徳川家の特別対応

対象範囲: 将軍家+御三家(尾張藩・紀州藩・水戸藩)+御三卿(田安家・一橋家・清水家)。

- **御三家(尾張・紀州・水戸)**: 普通の藩なので、既存の仕組み(藩主継承テンプレート+Wikidata)をそのまま使える。特別対応不要。
- **徳川将軍家**: 藩ではないため`{{○○藩主}}`のようなテンプレートはない。「疑似藩」として扱い、`han: "徳川将軍家"`、`generation`は将軍としての代数(初代家康〜15代慶喜)とする。データ取得元となるテンプレート(将軍の継承一覧に相当するもの)が実在するか、実装前に確認すること。
- **御三卿(田安家・一橋家・清水家)**: 領地を持たない分家で、藩主一覧テンプレートに相当するものが存在しない可能性が高い。各家の当主一覧は、家の記事本体の表や本文から抽出する必要があるかもしれない。既存の「テンプレートのlist1セクションを解析する」方式がそのまま使えない可能性が高いので、**実装前に必ずデータソースの形式を確認し、既存方式が使えるかどうか報告してもらってから**、抽出方法を決める。hanはそれぞれ`"田安徳川家"`,`"一橋徳川家"`,`"清水徳川家"`とする。
- **データ取得の役割分担**: 御三家(尾張・紀州・水戸)は普通の藩なので、これまで通りfetch_daimyo_data.pyでスクリプトから取得する。**将軍家と御三卿は対象人数が数十人程度と少なく、専用パーサーを書くコストに見合わないため、Claude(このプロジェクトの会話)が1件ずつWikipedia/Wikidataを調べながら手作業で`data/raw/tokugawa_shogun.json`・`data/raw/tayasu.json`・`data/raw/hitotsubashi.json`・`data/raw/shimizu.json`を作成する。その際も、他のraw ファイルと完全に同じスキーマ(id(QID), name, positions: [{han, generation, year_start, year_end}], wikipedia_url, image_url, father_id(QID), is_stub)に従い、父の判定はWikidata P22優先、画像はWikidata P18、というこれまでの方針をすべて踏襲する。こうすることで、既存のbuild_tree.pyは変更なしでこれらのファイルも取り込める。**
- **役職の併記(転封・徳川吉宗のような兼務への対応)**: 同一人物(同一QID)が複数の藩/将軍職の一覧に正式なメンバー(is_stub: false)として登場する場合(転封、または徳川吉宗=紀州藩主→8代将軍のようなケース)、どちらか一方を優先して選ぶのではなく、`positions` 配列に両方とも残す。表示側も「紀州藩5代目・将軍8代目」のように全役職を併記する(詳細はフェーズ2・フェーズ3の項を参照)。

## 代数の数え方(藩主家が途中で交代する藩への対応)

古河藩のように、同じ藩で藩主家が何度も交代する藩がある(小笠原家→戸田松平家→...→土井家、など)。Wikipediaのテンプレート表示は家ごとに「初代」とリセットされることがあるが、**このプロジェクトの`generation`は家の交代を無視し、その藩の藩主一覧全体を通しで1,2,3...と数える**(米沢藩・加賀藩と同じ、テンプレート内の並び順=代数、という考え方を貫く)。

また、このような藩は藩主家ごとの表形式(family名列でグループ化されたwikitable)でテンプレートが書かれていることがあり、これまで前提にしてきた「list1セクションの箇条書きリンクを順に抽出する」というシンプルな解析方法が使えない可能性がある。**対象藩を追加する際は、テンプレートが単純な箇条書き形式かどうかを毎回確認し、表形式の場合は別の解析ロジックが必要になる。**なお、藩主家が交代する藩は転封(他の藩からの転入・他の藩への転出)を伴うことが多く、`positions`配列のマージ(役職の併記)が発生しやすいことも踏まえておく。

## 対象藩の増やし方(運用手順)

1. 追加したい藩の藩主継承テンプレートが日本語版Wikipediaに存在するか確認する(例: `{{仙台藩主}}`)。藩主家が途中で交代した藩(会津藩など)はテンプレートの構成が単純でない場合があるので、事前に実物を確認する。
2. `scripts/fetch_daimyo_data.py` の対象藩リストにその藩を追加して実行する。既存の `data/raw/*.json` には影響しない。
3. `scripts/build_tree.py` を再実行し、`data/tree.json` を更新する。
4. ノード数・エッジ数・警告内容を確認し、問題なければコミットする。
5. 静的サイトなので、`data/tree.json` を更新してデプロイし直せば反映される。

## 進捗状況(随時更新)

- [x] プロジェクトフォルダ作成、README/.gitignore/ディレクトリ構成の雛形作成
- [x] GitHubリポジトリ Karintou83/edo-daimyo-genealogy をremoteに設定
- [x] フェーズ1スクリプト(データ取得) `scripts/fetch_daimyo_data.py` — Infobox(`父母`パラメータ内の実父/養父書き分け)ベースで実装済み
- [x] フェーズ1追加対応: 父の判定をWikidata P22最優先に切り替え、人物IDをQIDベースに変更済み。上杉茂憲の父もWikidata経由で解決済み(warningsに解決経緯が記録されている)。
- [x] フェーズ1再対応: 出力先を藩ごとの`data/raw/yonezawa.json`・`data/raw/kaga.json`に分割し、image_url(Wikidata P18経由)を追加済み。米沢藩17件(画像7件)・加賀藩14件(画像10件)。旧`data/daimyo_raw.json`はディスク上からは削除済み(gitへのコミットのみ未実施)。
- [x] フェーズ2スクリプト(データ整形): `scripts/build_tree.py` で `data/raw/*.json` を統合(QIDで重複排除)し `data/tree.json` を生成。31ノード/26エッジ/スタブ5件/image_urlあり17件、ID重複なしを実データで確認済み。
- [ ] スキーマ移行(進行中): `han`/`generation`(単一)を `positions: [{han, generation, year_start, year_end}]`(配列)に変更する。フェーズ1(fetch_daimyo_data.py)・フェーズ2(build_tree.py)の両方に影響。米沢藩・加賀藩は全員1役職のみなので、配列化しても実害なく移行できる見込み。在位年はテンプレート内の年数表記を優先し、なければWikidataのP39をフォールバックにする。この移行を先に済ませてから徳川家(将軍家+御三家+御三卿)に着手する。
- [ ] フェーズ3(表示サイト): `data/tree.json` を読み込んでD3.jsで表示する `index.html` を作る。`positions`を連結して表示(例:「紀州藩5代目・将軍8代目」)。ノードクリック時の詳細パネルには役職ごとの在位年も表示する。image_urlがあれば肖像画像を表示。色分けは`is_stub`で判定(既知事項を参照)。

## フェーズ2に向けた既知事項

- スタブノード(`is_stub: true`)は「対象藩の藩主一覧テンプレートに載っていない人物」という意味であり、「対象藩と無関係」という意味ではない。2種類ある。
  - 対象藩と無関係な他家・他藩の人物: 秋月種美(高鍋藩主)、長尾政景・吉良義央(藩制度確立前/大名ではない)、前田利家(藩制度確立前の藩祖)。これらは `positions` が実際の藩名を含むか、空配列(Infoboxに「藩」相当の記載がない場合)になる。
  - 対象藩の中にいるが藩主一覧には載らない人物: 上杉勝熙(米沢藩上杉家の世子だったが、藩主になる前に死去。父・治広と息子・斉定はともに米沢藩主)。この場合はInfoboxの記載から `positions: [{han: "米沢藩", generation: null}]` のように対象藩の藩名が正しく取れる。**これは正しい値なので空にしない。**
- `positions` が空配列になるのは、長尾政景・吉良義央・前田利家の3名(Infoboxに「藩」パラメータ自体が存在しないケース)。上杉勝熙のように `positions` が対象藩の名前を持つスタブノードもいる。
- フェーズ3の表示: 藩ごとの色分けは `is_stub: false` のノードにのみ適用する。`is_stub: true` のノードは `positions` の中身(空か対象藩名かに関わらず)、藩主とは別の見た目(グレーアウト・破線枠など)にする。フェーズ2のデータ整形自体では、`positions` の値をそのまま通せばよい(無理に埋めない・削らない)。判断材料として使うのは `is_stub` フラグの方。

## 実装上の注意点(フェーズ1で判明した実物の仕様)

- `{{基礎情報 武士}}`テンプレートでは、「父」は独立パラメータではなく`父母`パラメータの中に `父：[[実父名]]、母：[[母名]]<br/>養父：''[[養父名]]''` の形でラベル付きテキストとして埋め込まれている(上杉治憲・前田利常・上杉景勝で確認済み)。「養父」ラベルは明示的に除外し、「実父」「父」ラベルを優先する。
- 「藩」パラメータは `[[日向国|日向]][[高鍋藩]]主` のように国名リンク→藩名リンクの順で書かれることがある。藩名は「末尾が藩で終わるリンク」を優先して拾うこと(国名リンクを誤って藩名として拾うバグが過去にあった)。
- 明治以降に華族・政治家となった人物(例: 上杉茂憲)は `{{基礎情報 武士}}` ではなく `{{政治家}}` テンプレートが使われ、`父母` パラメータ自体が存在しないことがある。この場合、日本語版Wikipedia側では父が特定できないが、**Wikidata側にP22(父)が別途登録されている可能性がある**ため、フェーズ1追加対応(Wikidata優先化)で解消できる見込み。
- WikipediaのAPIには連続リクエストの間隔(1秒程度)を空け、適切なUser-Agentを設定すること。
- テンプレートの`list1=`セクション内でリンクを持たない項目(「廃藩置県」など)は代数としてスキップし、警告を出す(想定内の警告)。
- 藩主家が交代する藩(古河藩・佐倉藩など)は、テンプレートが`list1=`だけでなく`list2=`, `list3=`...と複数のリストに分かれていることがある。**存在するlist Nセクションをすべて順に読み、代数は通し番号で数える**(家の交代でリセットしない、というルールと整合させる)。
- テンプレート内のリンクには、藩主ではないもの(旧国名など。例: 佐倉藩のテンプレート内の「常陸国」、あるいは側室・家臣など藩主でない人物)が紛れ込むことがある。当初はWikidataのP31(instance of)が人間(Q5)かどうかで判定していたが、**この方式は撤回し、代わりに「藩主マスターリスト」による判定に切り替えた**: 日本語版Wikipediaの `Category:藩別の大名` を実際にブラウザで調査したところ、下位カテゴリとして `Category:○○藩主`(2026-09時点で328件、藩ごとに1つ)を持ち、各カテゴリは配下の藩主全員(藩主家が交代した場合も交代前後すべて)を直属ページとして持つことを確認した(会津藩主・宇和島藩主・上田藩主など、家の交代がある藩を含む複数例で検証済み)。一部の著名な藩主は自分の名前を冠したトピックカテゴリ(例: `Category:保科正之`。関連する寺社・郷土料理・古文書なども含む雑多なトピック集で、藩主ではない項目が混入する)を別途持つが、藩主本人は既に親の「○○藩主」カテゴリに直属ページとして含まれているため、**このトピックカテゴリへは再帰しない(1階層のみ辿る: 藩別の大名 → ○○藩主 → 直属ページ)**。直属ページには`Template:○○藩主`自体が混ざることがあるため標準名前空間(ns=0)のみに絞る。**藩主一覧テンプレートだけではカバーしきれない人物(知藩事、将軍、御三卿の当主)を補うため、以下の5カテゴリの直属ページ(ns=0、下位カテゴリへは再帰しない)もマスターリストに追加している**: `Category:知藩事`(版籍奉還〜廃藩置県の間、旧藩主がそのまま任じられた藩知事。下位カテゴリなし。「廃藩置県」等、人物でない直属ページも一部混在するが許可リストとして使うだけなので実害なし)、`Category:江戸幕府の征夷大将軍`(追贈された人物を含む。下位カテゴリ4件は将軍の御台所・側室・子女・トピックスで将軍本人ではないため再帰しない)、`Category:田安徳川家当主`・`Category:一橋徳川家当主`・`Category:清水徳川家当主`(御三卿3家それぞれの当主。いずれも下位カテゴリなし)。`Category:藩別の大名`配下を1階層だけ辿って集めた人物と、この5カテゴリの直属ページを集めた人物を合わせたID集合を「藩主マスターリスト」とし、`data/cache/daimyo_master_list.json`にキャッシュして使い回す(`--refresh-master-list`で再取得)。テンプレートから拾った候補リンクがこのマスターリストに含まれていなければ藩主候補から除外し、理由付きの警告を出す。実装は`scripts/fetch_daimyo_data.py`の`build_daimyo_master_list()`(取得元カテゴリは`MASTER_LIST_SUBCAT_CATEGORIES`/`MASTER_LIST_DIRECT_CATEGORIES`)を参照。
- **父の解決は最大5世代まで遡って探索する仕様(意図した挙動)**: 直接の父が対象藩の藩主一覧に含まれない場合、そこで即座にスタブ化するのではなく、父方をさらに最大5世代まで遡り、途中で(a)既知の藩主(対象藩の一覧、または既に取得済みの他藩の藩主一覧)、または(b)藩主マスターリスト(まだ取得していない藩の藩主も含む全藩主のID集合。詳細は上の「テンプレート内のリンク」の項を参照)に含まれる人物に行き着くかを確認する。**藩主マスターリストに含まれる人物に行き着いた場合は、その時点で探索を打ち切る**(5世代に達していなくても、藩主だと確認できた時点でそれ以上遡らない)。(a)に行き着いた場合はその人物は既にノードとして存在するので参照するだけでよいが、(b)(藩主マスターリストのみに含まれる、まだ未取得の藩の藩主)に行き着いた場合は、その人物自身がまだどこにもノードとして存在しないため、その人物自身も(父を持たない終端の)スタブノードとして新規に追加したうえで打ち切る(その藩を将来取得すれば、build_tree.pyのフェーズ2でスタブ→正式レコードに自動的に昇格する。秋月種美の例と同じ扱い)。どちらにも5世代以内に行き着かなければ、直接の父親1人だけをスタブノードとして表示し、途中で確認した中間世代の人物は表示しない(例: 武田信吉の父は徳川家康で、家康からさらに5世代遡っても対象藩の藩主にも藩主マスターリストにも行き着かなかったため、家康1人だけがスタブノードになる。家康自身は藩制度確立前の人物であり藩主マスターリストにも含まれない)。**これは意図した仕様であり、バグではない。**
