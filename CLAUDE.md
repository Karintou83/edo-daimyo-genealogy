# CLAUDE.md

このリポジトリで作業するときの前提・方針をまとめたもの。新しい会話やセッションでこのプロジェクトを開いたら、まずこのファイルを読むこと。

## プロジェクトの目的

江戸時代の藩主を血縁関係でつないだ、インタラクティブな家系図サイトを作る。最終的にGitHub(Pages)で公開する。

## 確定している設計方針

- **つなぐ関係**: 実際の血縁(親子)関係のみ。養子として藩主になった人物は、養父ではなく実父とつなぐ。
- **ノードの情報**: 「○○藩○代目」+氏名+その人物のWikipedia記事へのリンク、の3点で十分。生年没年・在任期間は不要。
- **データ取得元**: 対象人物と代数(藩主として何代目か)は、日本語版Wikipediaの藩主継承テンプレート(例: `{{佐賀藩主}}`)から取得する(テンプレート内の並び順=代数)。**父の情報はWikidataを最優先で使う**: 各記事のページ情報からWikidataのQID(項目ID)を取得し、WikidataのP22(父)プロパティを問い合わせる。P22が未登録の場合のみ、Wikipedia記事のInfobox(基礎情報系テンプレート)の「父」パラメータ(実父/養父の書き分けがあれば実父を優先)をフォールバックとして使う。ただしフォールバック時も、Infobox中のリンク先(表示テキストではなくリンクターゲットの記事名)から父の記事を特定し、その記事のQIDを取得して以降はQIDで扱う。
- **人物の識別**: すべての人物はWikidataのQIDを主キーとして扱う(氏名の文字列では照合しない)。同姓同名の人物が別々に存在しても、QIDで区別すれば取り違えない。記事にWikidata項目が存在しない稀なケースのみ、Wikipedia記事名をIDとして代用する。
- **サイトの構成**: データは一度取得してJSONファイルに保存し、サイトはそのJSONを読み込むだけの静的な構成にする。ページ表示のたびにWikipedia APIへ問い合わせるような、リアルタイム取得の仕組みにはしない。データを更新したい場合は取得スクリプトを再実行してJSONを作り直す。
- **表示**: D3.jsによるインタラクティブな家系図(ズーム・パン、ノードクリックで詳細パネル)。**色分けは「実際にその藩の藩主だったかどうか」で決める**: `is_stub: false`(藩主一覧テンプレートに載っている実際の藩主)のノードのみ、藩ごとの色を塗る。`is_stub: true` のノードは、`han` の値に関わらず(たとえ対象藩に属する人物であっても)藩主とは異なる見た目(グレーアウト・破線枠など)にし、藩主本人ではないことが一目でわかるようにする。
- **対象藩以外の実父の扱い**: 実父が対象藩の藩主一覧に含まれない場合(例: 上杉鷹山の実父・秋月種美は高鍋藩主)、その人物を「スタブノード」として追加する。氏名・Wikipediaリンク・(取得できれば)藩名のみを持たせ、そこからさらに祖先を遡って展開はしない。表示上も通常ノードと区別できるようにする(薄い色・簡易表示など)。

## プロトタイプ対象

まず数藩から検証する。選定理由もあわせて記載。

- 米沢藩(上杉家): 上杉鷹山が秋月家からの養子であり、養子ケース(実父リンク)の検証に使う
- 加賀藩(前田家): 直系継承が多く、シンプルなケースの検証に使う

対象藩を増やす際は、フェーズ1のスクリプトに藩名リストを渡せるようにしておくことで拡張する想定。

## 実装フェーズ

1. **フェーズ1(データ取得)**: `scripts/` に、Wikipedia上の藩主継承テンプレートとWikidataからデータを取得し `data/daimyo_raw.json` に保存するスクリプトを作る。各人物は `id(QID), name, han, generation, wikipedia_url, father_id(QID), is_stub: false` を持たせる。父の判定はWikidataのP22を最優先、未登録ならInfoboxの「父」パラメータをフォールバックとして使う(フォールバック時もリンクターゲットからQIDを解決する)。父が対象藩の藩主一覧に含まれない場合は、その父を `is_stub: true` の人物として追加し、`id(QID), name, wikipedia_url, han(取得できれば), generation: null` のみを持たせ、その人物の父はさらに遡って取得しない。
2. **フェーズ2(データ整形)**: `daimyo_raw.json` から `nodes`(id, name, han, generation, wikipedia_url)と `edges`(parent_id, child_id)を組み立て、`data/tree.json` に保存するスクリプトを作る。
3. **フェーズ3(表示)**: `tree.json` を読み込んでD3.jsで表示する `index.html` を作る。

## 進捗状況(随時更新)

- [x] プロジェクトフォルダ作成、README/.gitignore/ディレクトリ構成の雛形作成
- [x] GitHubリポジトリ Karintou83/edo-daimyo-genealogy をremoteに設定
- [x] フェーズ1スクリプト(データ取得) `scripts/fetch_daimyo_data.py` — Infobox(`父母`パラメータ内の実父/養父書き分け)ベースで実装済み
- [x] フェーズ1追加対応: 父の判定をWikidata P22最優先に切り替え、人物IDをQIDベースに変更済み。`data/daimyo_raw.json` は id(QID)/name/han/generation/wikipedia_url/father_id(QID)/is_stub のスキーマで、米沢藩・加賀藩あわせて31件(実際の藩主26名+スタブ5名: 長尾政景・吉良義央・秋月種美・上杉勝熙・前田利家)を出力済み。上杉茂憲の父もWikidata経由で解決済み(warningsに解決経緯が記録されている)。フェーズ1完了。
- [ ] フェーズ2スクリプト(データ整形): `daimyo_raw.json` → `data/tree.json`(nodes/edges化)
- [ ] フェーズ3(表示サイト)

## フェーズ2に向けた既知事項

- スタブノード(`is_stub: true`)は「対象藩の藩主一覧テンプレートに載っていない人物」という意味であり、「対象藩と無関係」という意味ではない。2種類ある。
  - 対象藩と無関係な他家・他藩の人物: 秋月種美(高鍋藩主)、長尾政景・吉良義央(藩制度確立前/大名ではない)、前田利家(藩制度確立前の藩祖)。これらは `han` が実際の藩名かnull(Infoboxに「藩」相当の記載がない場合)になる。
  - 対象藩の中にいるが藩主一覧には載らない人物: 上杉勝熙(米沢藩上杉家の世子だったが、藩主になる前に死去。父・治広と息子・斉定はともに米沢藩主)。この場合はInfoboxの記載から `han: "米沢藩"` のように対象藩の藩名が正しく取れる。**これは正しい値なので null に修正しない。**
- `han` が `null` になるのは、長尾政景・吉良義央・前田利家の3名(Infoboxに「藩」パラメータ自体が存在しないケース)。上杉勝熙のように `han` が対象藩の名前を持つスタブノードもいる。
- フェーズ3の表示: 藩ごとの色分けは `is_stub: false` のノードにのみ適用する。`is_stub: true` のノードは `han` の値(nullか対象藩名かに関わらず)、藩主とは別の見た目(グレーアウト・破線枠など)にする。フェーズ2のデータ整形自体では、`han` の値をそのまま通せばよい(無理に埋めない・削らない)。判断材料として使うのは `is_stub` フラグの方。

## 実装上の注意点(フェーズ1で判明した実物の仕様)

- `{{基礎情報 武士}}`テンプレートでは、「父」は独立パラメータではなく`父母`パラメータの中に `父：[[実父名]]、母：[[母名]]<br/>養父：''[[養父名]]''` の形でラベル付きテキストとして埋め込まれている(上杉治憲・前田利常・上杉景勝で確認済み)。「養父」ラベルは明示的に除外し、「実父」「父」ラベルを優先する。
- 「藩」パラメータは `[[日向国|日向]][[高鍋藩]]主` のように国名リンク→藩名リンクの順で書かれることがある。藩名は「末尾が藩で終わるリンク」を優先して拾うこと(国名リンクを誤って藩名として拾うバグが過去にあった)。
- 明治以降に華族・政治家となった人物(例: 上杉茂憲)は `{{基礎情報 武士}}` ではなく `{{政治家}}` テンプレートが使われ、`父母` パラメータ自体が存在しないことがある。この場合、日本語版Wikipedia側では父が特定できないが、**Wikidata側にP22(父)が別途登録されている可能性がある**ため、フェーズ1追加対応(Wikidata優先化)で解消できる見込み。
- WikipediaのAPIには連続リクエストの間隔(1秒程度)を空け、適切なUser-Agentを設定すること。
- テンプレートの`list1=`セクション内でリンクを持たない項目(「廃藩置県」など)は代数としてスキップし、警告を出す(想定内の警告)。
